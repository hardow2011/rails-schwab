<!DOCTYPE html>
<html>
  <head>
    <title>Schwab</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= yield :head %>
  </head>

  <body class="vh-100">
    <% if logged_in? %>
      <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <%= link_to 'Schwab', root_url, class: 'navbar-brand' %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse justify-content-end pt-3" id=navbarNavDropdown>
            <ul class="navbar-nav mb-2">
              <li class="nav-item"><%= link_to 'Account', user_url, class: 'nav-link text-end' %></li>
              <li class="navbar-logout d-flex"><%= button_to "Logout", sessions_path, class: 'nav-link w-100', method: :delete %></li>
            </ul>
          </div>
        </div>
      </nav>
    <% end %>
    <div class="container-fluid h-100">
      <%= yield %>
    </div>
  </body>
</html>
